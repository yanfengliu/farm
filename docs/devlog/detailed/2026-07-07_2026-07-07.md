# Detailed Devlog: 2026-07-07 to 2026-07-07

## 2026-07-07

- Action: Re-inspected the LLM/player playtest harness action surface.
- Result: Scripted scenario observations now use the same player-facing selector set as the visual loop, including canvas, buttons, range inputs, numeric inputs, role buttons, and resize handles. Both harness observation packets now include action hints plus player-visible state such as active controls, shortcuts, and input values, and the Markdown/HTML reports render that state for review.
- Reasoning: A live DOM comparison showed the visual loop could act on the new numeric crop-mix fields, but the scripted review packet omitted canvas and numeric input controls from its available action list. That made the harness less able to "see" what a player could use, even though the scripted tour could still click and fill those controls.
- Validation: Added failing harness/evaluator contracts for full action-surface extraction and visible control state, watched them fail, then patched both harnesses and report rendering. Reran focused contracts green, live-compared the action packet against the DOM, and reran both LLM playtest harnesses against localhost 5175 with zero findings. External review was unavailable in-turn, so an in-process adversarial review checked selectors, action execution boundaries, report output, and screenshots.

- Action: Tightened LLM/player observations to match screenshot visibility.
- Result: Both harnesses now extract visible text from viewport- and scroll-clipped text fragments instead of `document.body.innerText`, expose the side-panel content as a player wheel-scroll target with scroll state, and the scripted playtest captures a `side-panel-scrolled` screenshot. The visual loop default now allows enough decisions to scroll the side panel, claim Tier 2, open Crop Mix, and type a numerical Wheat allocation.
- Reasoning: A full harness inspection showed the LLM could receive offscreen Goals text that a player had not scrolled to, while the action list did not let the LLM wheel the side panel to reach that content legitimately. That was too omniscient for a visual playtest harness.
- Validation: Added failing harness contracts for viewport-only text extraction, side-panel scroll action exposure, scripted panel-wheel coverage, the larger visual-loop decision budget, and keeping panel wheel-scroll separate from camera zoom coverage. Watched them fail, implemented the player-scroll region and visible-text extraction in both harnesses, then reran the focused contracts green. Reran `npm run playtest:llm:visual-loop` and `npm run playtest:llm` against localhost 5175 with zero findings; visual review covered scrolled Goals, compact desktop, and Crop Mix before/after the numeric Wheat edit.

- Action: Exposed keyboard-only controls in LLM/player observations.
- Result: Scripted and visual-loop observation packets now include `keyboardActions` for Arrow/WASD camera panning and Space pause/resume, and the Markdown/HTML reports render those controls alongside DOM/canvas actions.
- Reasoning: A harness reinspection showed the action schema could execute `press` actions, but an external LLM player only saw DOM/canvas controls in the observation packet. Camera panning was therefore covered by the local heuristic, but not fully discoverable to an LLM acting from the screenshot prompt.
- Validation: Added failing harness/report contracts for keyboard action enumeration and Markdown rendering, watched them fail, implemented the observation/report fields, and reran the focused contracts green. A visual check of the HTML replay viewer also exposed that the screenshot frame was pushed below the viewport by the metadata rail; added a failing viewer-shell contract, fixed the viewer to use a fixed 100vh shell with a scrolling metadata rail, reran the visual loop with zero findings, and captured a corrected viewer screenshot.

- Action: Added side-panel scroll affordances for dense panel content.
- Result: The side panel now tracks whether content can scroll up or down, renders lightweight top/bottom fades, reserves a slim scrollbar gutter, and gives bottom content extra breathing room above the toolbar.
- Reasoning: A fresh visual-loop Goals screenshot showed the lower progression row fading into the toolbar edge with no obvious scrollbar in Chromium's overlay-scrollbar environment, making intentional scrollable content look like accidental clipping.
- Validation: Added a failing browser polish test for scroll-state classes, watched it fail, implemented the affordance, reran the focused test and the full visual-polish suite green, reran the visual LLM loop against localhost 5175 with zero findings, and visually checked top and bottom Goals screenshots.

- Action: Added a dedicated scripted LLM Inspect-panel capture.
- Result: The scripted player tour now screenshots the Inspect panel immediately after selecting a visible tile through the canvas, before returning to Goals.
- Reasoning: The harness previously controlled Inspect but captured only the later Goals state, so LLM review could not judge whether selected tile/object details were readable or useful.
- Validation: Added a failing harness contract for the Inspect capture order, watched it fail, added the `inspect-tile` scenario, reran the focused contract green, and reran the scripted LLM playtest against localhost 5175 with zero findings. The new screenshot exposed that well details are currently sparse, which is the next improvement target.

- Action: Expanded Inspect panel details.
- Result: Inspect now renders compact labeled detail rows for workers, locked land, empty land, plots, wells, and storage bins. The starter well inspection now explains that it is a water source, workers refill there, and it blocks movement.
- Reasoning: The new `inspect-tile` screenshot showed the selected well panel only said `Kind: well`, which was technically correct but not useful enough for a player trying to understand logistics.
- Validation: Added a failing browser visual-polish test for selected well details, watched it fail, implemented the richer Inspect rows, reran the focused test green, reran the scripted LLM playtest against localhost 5175 with zero findings, and visually checked the updated `inspect-tile.png` screenshot.

- Action: Re-audited player-like LLM harness keyboard coverage.
- Result: Scripted and step-by-step visual observations now enumerate visible toolbar shortcut keys in addition to camera controls, including tool selection, undo/redo, pause, and speed keys with their originating button selectors and state. The visual loop now uses the visible Plot shortcut during first-run guidance, proving a `press` decision can follow a real toolbar hotkey path.
- Reasoning: The harness could execute arbitrary key presses and the toolbar displayed shortcut badges, but an external LLM player only saw camera keys and Space in the dedicated keyboard action list. That left a real player control path discoverable only by reading tiny text inside button labels rather than as a first-class action.
- Validation: Added failing harness-contract tests for visible toolbar shortcut enumeration, Plot shortcut choice, and skipping the held Select Plot guide after a post-shortcut canvas click. Watched them fail, then patched both observation collectors and the visual-loop heuristic. Reran focused contracts green, reran `npm run playtest:llm:visual-loop` and `npm run playtest:llm` against localhost 5175 with zero findings, visually checked the shortcut/action screenshots, and reran `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` green.

- Action: Added canvas drag-paint coverage to the LLM/player harness.
- Result: The scripted player-surface tour now performs a real mouse drag over the canvas with the Plot tool selected. The visual loop now labels canvas actions as click-or-drag targets, accepts screenshot-relative drag start coordinates, and its local heuristic uses a visible canvas drag after Plot is selected.
- Reasoning: The game supports drag painting through the same pointer events a player uses, but the harness described canvas control as click-only and the scripted tour only clicked individual cells. That left a real tool-control path under-tested.
- Validation: Added failing harness-contract tests for scripted canvas dragging and visual-loop canvas drag execution, watched them fail, then implemented the drag helper and start-coordinate normalization. Reran focused contracts green, reran `npm run playtest:llm:visual-loop` and confirmed a live `drag` decision with zero findings, reran `npm run playtest:llm` and confirmed the scripted player action log includes a canvas drag with zero findings, then reran `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` green.

- Action: Re-inspected selector-focused keyboard parity in the LLM/player harness.
- Result: Scripted and visual-loop observations now list focus-required keyboard actions for the side-panel resizer and visible crop-mix range sliders. The scripted surface tour presses the resizer handle through the keyboard, and the visual-loop normalizer rejects unlisted press keys before execution.
- Reasoning: A harness audit found the LLM player could see mouse resize and global shortcuts, but not the resizer's Arrow/Home/End keyboard path. The visual-loop provider boundary also trusted arbitrary key strings despite the prompt saying to use listed keyboard controls only.
- Validation: Added failing harness-contract tests for selector-focused keyboard action discovery, scripted keyboard resizing, and press normalization against the observed keyboard action list. Watched them fail, then patched the observation collectors, scripted tour, and visual-loop executor. Reran the focused contracts green, reran `npm run playtest:llm:visual-loop` and `npm run playtest:llm` against localhost 5175 with zero findings, confirmed the reports include focus-required resizer keyboard actions, and visually checked the initial, Crop Mix, final, Inspect, and surface-tour screenshots.

- Action: Made the visual LLM-player exercise a focused keyboard control by default.
- Result: The local visual-loop heuristic now focuses the visible side-panel resize handle and presses ArrowLeft once after camera pan/zoom coverage, proving selector-focused press execution in the replay instead of only listing the action.
- Reasoning: The previous audit confirmed an LLM could see and choose focus-required keyboard controls, but the default no-key provider replay still did not demonstrate that path. Exercising it locally keeps the visual loop honest when no external LLM provider is configured.
- Validation: Added a failing harness-contract test for the focused resize-keyboard heuristic, watched it fail, implemented the minimal decision path, reran the focused visual-loop contract green, reran `npm run playtest:llm:visual-loop` against localhost 5175 with zero findings, confirmed step 3 is a focus-required `[data-panel-resizer]` ArrowLeft press, and visually checked the post-resize, Crop Mix, and final screenshots.
