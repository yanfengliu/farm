# 2026-07-13

## Self-directed duck ecology

- Result: Pip and Mallow are deterministic simulation actors with individual hunger, energy, activity, habitat location, destination, travel progress, and meal counts. Hungry ducks reserve different available fish, travel to the matching creek habitat, eat, and begin that fish's renewable respawn; tired ducks release food reservations, seek a grove shelter, sleep under its tree canopy, and wake rested without player input.
- Determinism evidence: focused simulation contracts cover distinct reservations, feeding and respawn, shelter and wake transitions, array-order independence, JSON save/recreate continuation, 10,000-tick stability, pause behavior, and readable debug projection.
- Content boundary: habitat IDs, duck identities, initial needs, and all lifecycle thresholds and durations live in `src/game/content/wildlife.ts`; simulation rules live in `src/game/simulation/wildlifeSystem.ts`; Phaser only maps semantic habitat nodes to pixels and draws state-specific poses.

## Creek and habitat art

- Result: the recurring creek shape is now unmistakably a lily pad rather than an unexplained flower. Stable coordinate hashes vary channel inset, vertical spacing, pad size, notch direction, companion leaf, blossom presence, and cream, pink, or lavender blossom palette while keeping screenshot evidence reproducible.
- Result: Pip has a gold-and-green palette and Mallow has a cream-and-cinnamon palette. Swimming leaves wakes, foraging dips the bill into ripples, shore travel shows feet, eating shows fish glints, and sleeping tucks the duck into a leaf bed with a small `Z` beneath the canopy of the same grove trees used by environment art.
- Browser evidence: dedicated contracts exercise initial, feeding, shelter, and paused states at 1280x800 and 1024x720, check both ducks remain visible and distinct, and prove duck actor pixels remain stable while paused.

## Save, history, and replay compatibility

- Result: version-1 saves accept missing wildlife as an additive legacy field, normalize both active and Undo/Redo snapshots, and validate reciprocal fish reservations plus activity, location, target, timer, and travel-progress coherence before simulation use.
- Result: old replay snapshots that never contained wildlife remain historically exact: replay application, stepping, text projection, and legacy Undo preserve the absence instead of crashing or inventing state. Old local saves still gain the authored starter ecology, matching additive migration expectations.
- Evidence: the committed pre-ecology `latest.bundle.json` replays strongly with one checked segment and zero skipped or divergent segments after the change.

## Adversarial ecology hardening

- A code-reading gameplay reviewer proved the original critical-fatigue branch was unreachable in natural play and that structurally coherent but truncated wildlife arrays could permanently remove Mallow or a creek fish. Fail-first boundary fixtures now force a reserved duck across the actual shelter threshold and remove authored roster members; the simulation releases the meal before shelter routing, while save validation requires the exact duck ID/name and fish ID/habitat roster whenever the ecology field exists.
- A live renderer review measured a 36.3-screen-pixel north-to-south wrap and then caught that the first long-route repair still allowed a 10.05-world-pixel normal mid-creek hop. Roaming now reverses at creek endpoints, long routes scale by habitat distance, base travel advances at a slower cozy cadence, and the final renderer contract enumerates every creek/tree node pair against the real pixel projection with a six-world-pixel maximum step.
- Independent re-review proved the fatigue and roster findings resolved. The final movement finder rechecked the complete node-pair contract after the base-speed correction rather than relying on the earlier long-route-only fixture.

## Integrated verification

- Full gates: `npm test` passed 26 files and 261 tests; `npm run typecheck`, `npm run lint`, and `npm run build` passed. Vite retains the known Phaser bundle advisory at 1,393.17 kB minified / 387.88 kB gzip; it is a performance follow-up rather than a correctness failure.
- Visual evidence: browser contracts verified the ecology at 1280x800 and 1024x720, including a sleeping duck under the shared grove canopy, its fishing companion, irregular creek vegetation, and exact paused duck-pixel stability. The final recursive screenshot at `output/playwright/llm-visual-loop/steps/139-step-139.png` was inspected for framing, overlap, creek readability, ducks, and UI fit.
- Recursive evidence: final visual run `farm-visual-loop-2026-07-13T18-32-10-936Z` consumed all 140 decisions at Tier 4 with four workers, no console or page errors, no engine findings, and no improvement findings. Its retained ticks 6336-6400 replay checked one command segment with zero skipped segments and zero state, event, or execution divergences.
- Scope honesty: wrapper `farm-recursive-2026-07-13T18-32-08-669Z` reports `no-fix-candidate` only for `local-heuristic-full-surface-140-step`; the visual run ended at its ceiling rather than agent-stopping, and the manifest explicitly leaves broader quality `not-evaluated` with `broaden-discovery` next action.
- Review evidence: independent in-process reviewers re-read simulation, persistence, rendering, tests, and docs against the live tree and found no blocking defect after the integrated fixes. Codex CLI 0.144.1 and Claude CLI 2.1.207 both failed their fleet-canonical external review attempt with HTTP 401, so no external reviewer approval is claimed.

## Farm Notes proof hardening and final re-drive

- Result: an open Farm Notes draft now owns the complete gameplay input boundary, including canvas clicks, toolbar commands, global shortcuts, simulation time, and reset. Stored note indices require safe integers and allocate collision-free identities across malformed counters and the maximum-safe wrap boundary.
- Harness result: the deterministic visual player completes the create/cancel/recapture/view/pin/edit/copy/export/confirmed-delete lifecycle, counts only successful executions, performs Undo/Redo immediately around the intended plot command, compares the uncapped set of open coverage classes even though reports display only the top findings, and treats generated note-pin selectors as one semantic cross-run coverage family.
- Artifact result: recursive wrappers freeze a closed proof packet with rewritten screenshot and bundle references instead of leaving old manifests dependent on mutable `latest` files. Embedded prompt paths are rewritten in native, forward-slash, and JSON-escaped forms as well as the explicit screenshot fields.
- Artifact evidence: wrapper `farm-recursive-2026-07-14T00-05-08-979Z` records clean revision `23f03ae4a8bdaf1faa68e5e6a4cb6696cbc79174` and points to an immutable directory containing JSON, Markdown, HTML, replay bundle, manifest, and 147 screenshots. Every manifest artifact exists; all 147 unique screenshot references resolve inside that directory; no stored string retains a canonical screenshot path; archived and canonical screenshot SHA-256 hashes have zero mismatches; and the archived replay bundle matches canonical bytes.
- Recursive evidence: visual run `farm-visual-loop-2026-07-14T00-05-11-509Z` agent-stopped after completing 146 of at most 160 decisions with no findings, zero open coverage ids, and no console, page, or engine errors. Retained ticks 5568-5632 replay checked one command segment with zero skips or state, event, and execution divergences.
- Scope honesty: the canonical comparison had no usable prior `latest` packet after the interrupted pre-fix audit and records `no-baseline`; this pass is current-state verification rather than before-after comparison evidence. The wrapper reports `no-fix-candidate` only for `local-heuristic-full-surface-160-step`; broader quality remains `not-evaluated` with `broaden-discovery` next.
- Visual evidence: independent inspection at 1280x800 and 1024x720 found the annotation aim, draft, list, edit, and pin surfaces readable without horizontal overflow, clipped action labels, or control overlap. Exhaustive decoding proved the apparent black preview tiles contained zero black, near-black, or transparent pixels and were preview-tool artifacts rather than game or PNG corruption; trees, plants, creek, workers, ducks, plots, and Tier 4 scenery remained coherent.
- Verification: `npm test` passed 34 files and 303 tests; `npm run typecheck`, `npm run lint`, and `npm run build` passed. Vite retains the non-blocking Phaser bundle-size advisory. Standalone Codex authentication is valid, but private-repository export policy blocked external Codex and Claude review, so no external approval is claimed; independent in-process reviewers found and reverified the prompt-reference and generated-selector fixes against the live tree.

## Player-annotated hedgerow correction

- Result: the exported Box note `What is this? It looks un-natural` pinpointed the east mixed hedgerow rather than requiring a verbal reconstruction. The former seed-19 sequence rendered five equal-width, equally spaced dark rectangles with repeated highlights; the replacement composes rounded overlapping leaf clusters along a crooked rise, with uneven bases, broken shadows, ground sprouts, sparse flowers, and occasional woody shoots.
- Debugging result: farm hedgerow placements and visual bounds now have one deterministic source shared by Phaser rendering and semantic annotation picking. The center of the original note returns `hedgerow:east`, label `Wild Hedgerow`, rather than the fallback `Meadow / 15,5` target.
- Regression evidence: the layout test uses the real production anchor and pins deterministic variation, neighbor continuity, compact lobe proportions, actual renderer fill widths, a non-strip footprint, and four pixels of breathing room inside the default recenter view. Injecting one shrub-width renderer rectangle made the new render contract fail at 22 pixels against a 15.84-pixel ceiling before the mutation was removed. Expanding the global scenery frame to cure the first clipping candidate broke six existing player-coordinate and composition contracts; compacting this placement instead preserved the established camera and all 46 focused annotation/art-direction checks. The annotation target test replays the original world center. Headless screenshots cover the complete thicket at starter framing 1280x800 and 1024x720 plus the attachment's exact 2560x1305 camera after a settled render.
- Verification: `npm test` passed 38 files and 327 tests; `npm run typecheck`, `npm run lint`, and `npm run build` passed. The build retains the established non-blocking Phaser chunk-size advisory.
