# 2026-07-12

## Base hardening before post-MVP content

- Result: malformed and partial version-1 local saves now fail closed to a fresh farm instead of reaching simulation normalization with missing nested state. Valid current saves and the intentionally supported additive upgrade/stat migration still load.
- Evidence: `tests/persistence/localSave.test.ts` first failed because `{ "version": 1 }` was accepted, then passed after the structural guard covered tiles, workers/tasks/cargo, inventory, crop mix, tier, stats, alerts, and history.
- Result: every seed restock control now quotes the exact affordable quantity and total click cost, carries that amount through the DOM action, and remains capped and revalidated by the simulation command.
- Evidence: the new inventory browser contract first saw the ambiguous `Buy Carrot seeds` / `1c` control, then passed with `Buy 5 Carrot seeds for 5 coins`, `+5 · 5c`, and an observed five-seed/five-coin state delta.
- Result: browser code now imports the supported `civ-engine/browser` barrel; `package-lock.json` matches the installed sibling engine at 2.4.1; scripts are covered by ESLint with explicit Node/browser globals; the README reflects port 5175 and the live tool mapping.
- Dependency audit: `npm audit --audit-level=high --omit=dev` and `npm audit --audit-level=high` both reported zero vulnerabilities.

## Village Harvest content and progression

- Added twelve authored Village Requests across Tiers 2 through 4. Each tier deterministically presents two neighbor baskets, the player may pin one, and delivery consumes the exact crops for a premium before rotating the board. There are no timers, penalties, or hidden randomness.
- Added request acceptance, abandonment, and fulfillment as typed simulation commands with undo/redo support, text-debug projection, lifetime request statistics, and migration defaults for both the active snapshot and historical snapshots.
- Replaced the old Tier 3 terminal with a three-request plus ten-harvested-tomato milestone. The tomato requirement prevents request progress earned in Tier 2 from skipping Tier 3's crop loop. Claiming Tier 4 Harvest Hearth unlocks pumpkins, four pumpkin seeds, a fourth farmhand, and an open-ended harvest loop.
- Added a dedicated Requests panel and Farm Guide steps for meeting the village, pinning the first basket, following basket progress, and delivering it. Locked Tier 1 players see the exact Wheat Rows unlock condition.
- TDD evidence: `tests/simulation/communityRequests.test.ts` covers deterministic offers, pin/abandon/deliver semantics, rewards, rotation, stats, milestone readiness, and replay behavior; `tests/browser/communityRequests.test.mjs` covers real visible controls, locked copy, and guide routing; persistence contracts cover legacy crop/request/history normalization.

## Sunlit patchwork art direction

- Split Phaser orchestration from the renderer and separated meadow, ground, object, actor, effect, and interaction graphics. The presentation clock and hashed decorative variation remain renderer-only and never enter save state.
- Replaced the dark empty stage with a textured meadow surrounding the simulation grid, a flowing creek and stepping-stone bridge, cottage and chimney, hedges, trees, flowers, reeds, farm sign, perimeter fencing, and small ambient glints.
- Redrew soil beds, moist/dry furrows, growth-stage crops, pumpkin vines and fruit, wells, storage bins, and four stable farmhand palettes with directional walk poses, hats, cargo, and task props. Placement previews now use translucent fill and pixel corner brackets.
- Shifted DOM chrome to warm espresso glass, cream text, and harvest-gold accents, and split the large stylesheet into focused base, component, request, toolbar, and tutorial files.
- Headless browser evidence: the dedicated cozy-art contract checks meadow coverage and recognizable orange pumpkin pixels at 1280x800 and 1024x720; the visual loop screenshot at `output/playwright/llm-visual-loop/steps/01-final.png` was inspected for farm framing, readable text, and overlap before the complete recursive pass.
- Kept the illustrated landmarks outside the buyable grid, bounded panning and zoom to the meadow, added a discoverable Home recenter action, and let ambient creek, well, and glint pixels animate on presentation time even while the deterministic farm is paused.

## Maintainability boundary work

- Split the simulation into types, normalized state construction, progression selectors, and farm systems; split DOM rendering into pixel icons, request, crop-mix, inspect, farm-row, panel-state, and tutorial modules; reduced `src/main.ts` to browser composition and event wiring.
- All new gameplay tuning remains in `src/game/content/`, and all current TypeScript and CSS files remain below the repository's 500-line target after the final style split.
- The in-process UI extraction review found two inherited keyboard/screen-reader defects. Fail-first browser contracts now prove that Space activates a focused panel button without also pausing the farm and that the completed-request stamp announces its count; the fix excludes interactive controls from global shortcuts, releases Phaser's Space capture, and adds a pluralized status label.

## Adversarial hardening

- Independent code-reading reviews found unbounded command history, localStorage quota failures escaping the UI, incomplete save-shape checks, a possible fourth-worker spawn collision, duplicated request progression counters, Crop Mix rerenders that could destroy an active edit, camera escape/focus conflicts, simulation-bound ambience, and permanent scenery that full expansion could overwrite.
- The integrated fixes cap Undo/Redo at 100 entries, fail persistence operations safely with a visible HUD warning, validate owned-map connectivity and worker paths, require integer item quantities, choose an unoccupied walkable worker spawn, reconcile community counters conservatively, preserve focused Crop Mix controls until commit, contain and recenter the camera, animate ambience on presentation time, and move scenic landmarks beyond buildable land.
- Regression evidence includes the persistence rejection and quota-failure suites, deterministic request replay through accept/fulfill/Tier 4/Undo/Redo, real multi-digit number editing and 30-step pointer dragging, focused-key camera isolation, Home/reset framing, paused ambience, and fully expanded landmark visibility.
- External Codex and Claude CLI review processes were attempted with the fleet-canonical prompts, but both returned HTTP 401 authentication failures. The task therefore used the parallel in-process code-reading reviewers and direct driver verification; no external approval is claimed.

## Integrated verification

- `npm test`: 22 files and 200 tests passed, including simulation determinism, structural save rejection, legacy save/history migration, persistence failure handling, request interaction and replay, both desktop viewports, camera containment, paused ambience, tutorial layout, real crop-mix editing, keyboard activation, and visual-loop contracts.
- `npm run typecheck`, `npm run lint`, and `npm run build`: passed. Vite retains the known Phaser bundle-size advisory at 1,353.97 kB minified / 375.45 kB gzip; it is a performance follow-up rather than a correctness failure.
- `npm audit --audit-level=high --omit=dev` and `npm audit --audit-level=high`: zero vulnerabilities.
