# 2026-07-12

## Base hardening before post-MVP content

- Result: malformed and partial version-1 local saves now fail closed to a fresh farm instead of reaching simulation normalization with missing nested state. Valid current saves and the intentionally supported additive upgrade/stat migration still load.
- Evidence: `tests/persistence/localSave.test.ts` first failed because `{ "version": 1 }` was accepted, then passed after the structural guard covered tiles, workers/tasks/cargo, inventory, crop mix, tier, stats, alerts, and history.
- Result: every seed restock control now quotes the exact affordable quantity and total click cost, carries that amount through the DOM action, and remains capped and revalidated by the simulation command.
- Evidence: the new inventory browser contract first saw the ambiguous `Buy Carrot seeds` / `1c` control, then passed with `Buy 5 Carrot seeds for 5 coins`, `+5 · 5c`, and an observed five-seed/five-coin state delta.
- Result: browser code now imports the supported `civ-engine/browser` barrel; `package-lock.json` matches the installed sibling engine at 2.4.1; scripts are covered by ESLint with explicit Node/browser globals; the README reflects port 5175 and the live tool mapping.
- Dependency audit: `npm audit --audit-level=high --omit=dev` and `npm audit --audit-level=high` both reported zero vulnerabilities.
