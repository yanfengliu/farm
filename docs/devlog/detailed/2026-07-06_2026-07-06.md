# Detailed Devlog: 2026-07-06 to 2026-07-06

## 2026-07-06

- Action: Added operational context to the Crop Mix panel after a visual LLM playtest loop.
- Result: Each crop-mix row now keeps its slider but also shows seed stock, planted plot count, and a compact readiness state such as no seeds stocked, needs empty plots, ready for workers, or locked until a later tier. The panel refresh signature now includes mix-relevant seed, plot, and lock state so those details stay current while the panel is open.
- Reasoning: The visual loop naturally landed on Crop Mix after claiming Tier 2. The sliders were legible, but the panel did not explain why workers might still plant carrots, ignore wheat, or stall after a new crop unlock. Showing readiness next to the target percentages makes crop planning understandable without sending players back to Inventory or Goals.
- Validation: Added a failing browser visual-polish regression for Crop Mix seed stock, readiness status, and panel fit, watched it fail on missing row metadata, then implemented the row details and CSS. Reran the focused browser regression green and reran `npm run playtest:llm:visual-loop`; it completed 20 player-facing steps with zero findings. The final visual-loop screenshot confirmed the richer Crop Mix rows fit in the side panel.
- Action: Prevented stale Crop Mix Farm Guide cards.
- Result: Opening the Crop Mix panel after multiple crops are unlocked now marks the Crop Mix guide step as seen, even when a different guide card was active at that moment.
- Reasoning: The full LLM/player harness opened and adjusted Crop Mix, then later sold crops and saw a redundant Tune Crop Mix card. The player had already used the panel, so the guide should advance to the real blocker instead of replaying a completed instruction.
- Validation: Added a failing browser regression for opening Crop Mix under a different active guide and then clearing sellable crops, watched it fail with a stale `open-mix-panel` card, implemented the panel-selection fix, and reran the tutorial suite green. Reran `npm run playtest:llm`; post-sale screenshots now point to Select Plot instead of Tune Crop Mix, with zero findings.
- Action: Clarified locked seed purchases in Inventory.
- Result: Locked Wheat and Tomato seed controls now render as disabled Locked buttons with locked aria labels and titles instead of showing their future seed prices.
- Reasoning: The fresh-start visual loop screenshot showed disabled seed buttons with prices for crops the player had not unlocked. That made lock state look like an unexplained affordability or clickability problem.
- Validation: Added a failing browser regression for locked seed labels, watched it fail against the price-only buttons, implemented locked labels, reran the focused regression green, and reran `npm run playtest:llm:visual-loop`; the fresh-start screenshot shows the Locked labels fitting inside the Inventory panel with zero findings.
- Action: Added visible milestone progress counts.
- Result: The HUD alert and Goals Next milestone copy now show current/target harvest progress for carrot and wheat tiers, such as `Harvest 0/20 wheat`, while the open-ended tier keeps its existing copy.
- Reasoning: The Tier 2 Goals screenshot said only `Harvest 20 wheat`, which did not tell players or the visual harness whether the farm was making progress during waits.
- Validation: Added a failing browser visual-polish regression for `Harvest 7/20 wheat` in both HUD and Goals, watched it fail against static milestone copy, implemented the milestone progress helper and Goals panel signature update, reran the focused regression green, and reran `npm run playtest:llm`; Tier 2 screenshots now show `Harvest 0/20 wheat` with zero findings.
- Action: Reserved the 5175 localhost lane for Farm playtesting.
- Result: `npm run dev` now starts on `http://127.0.0.1:5175/` with a strict port, both LLM harnesses default away from 5173, and `FARM_PLAYTEST_URL` can attach them to an already-running Farm server.
- Reasoning: The visual-loop report still identified `http://127.0.0.1:5173/`, which conflicts with the user's AoE localhost lane and makes playtest evidence easy to misread.
- Validation: Added a failing harness contract for the 5175 dev/playtest default, watched it fail against the old 5173 command, then updated the scripts, fallbacks, and docs. Reran the full harness contract, `npm run playtest:llm:visual-loop`, and `npm run playtest:llm` against `FARM_PLAYTEST_URL=http://127.0.0.1:5175/`; both reports recorded the 5175 URL with zero findings.
- Action: Prioritized visible tier-claim Farm Guide cards.
- Result: Higher-priority progression guidance can interrupt the readable-hold window for lower-priority sell or planning cards, so a visible Claim Rewards button is paired with a Claim Tier guide.
- Reasoning: The Tier Ready Goals screenshot still showed an Open Inventory guide because the stable card hold preserved a lower-priority sell prompt into the reward moment.
- Validation: Added a failing browser regression that held an Open Inventory guide in Goals, advanced the farm into a claimable tier, and expected a Claim Tier card. It failed with `open-inventory-for-selling`, then passed after adding explicit guide priorities. Reran the full tutorial suite plus both 5175 LLM playtest harnesses; the Tier Ready screenshots now show `Claim Tier 2` beside the Claim Rewards button with zero findings.
- Action: Tightened visual-loop seed guidance detection.
- Result: The local visual-loop heuristic only clicks seed buys when the visible state includes explicit seed-restock guidance such as `FARM GUIDE Buy Seeds` or `Farmers Waiting`, not incidental words in another guide's Why copy.
- Reasoning: The step log showed the heuristic buying carrot seeds during a Sell Crops guide because it matched `Coins buy seeds` inside the explanation text.
- Validation: Added a failing harness contract that rejected the broad seed regex, updated the heuristic, reran the full visual-loop contract, and reran `npm run playtest:llm:visual-loop` against 5175. The new report sells crops during the Sell Crops card and waits for an explicit Buy Seeds guide before buying seeds, with zero findings.
- Action: Corrected the Select Plot Farm Guide shortcut.
- Result: The Select Plot guide now says `Press 1 or click Plot in the toolbar`, matching the bottom toolbar binding.
- Reasoning: The compact desktop screenshot showed the guide telling players to press 2 even though Plot is bound to 1 and Well is bound to 2.
- Validation: Updated the existing readable-hold browser regression to press and expect 1, watched it fail against the stale copy, fixed the guide text, reran the focused regression green, and regenerated the compact scripted playtest screenshot on 5175.
