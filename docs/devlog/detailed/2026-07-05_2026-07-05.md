# Detailed Devlog: 2026-07-05 to 2026-07-05

## 2026-07-05

- Action: Added first-time next-click tutorial tips and clarified idle planting blockers.
- Result: The UI now renders one compact tutorial callout at a time, anchored to the next relevant click for tier claiming, seed restocking, plot painting, crop selling, and crop-mix tuning. Crop and seed rows now use crop icons. The simulation also alerts when idle workers have desired seeds but no empty plots to plant.
- Reasoning: A player with carrot seeds and a 75% carrot mix expected workers to keep planting carrots. The simulation already does that when empty plots exist, but the UI did not explain the separate "no planting space" stall. The tutorial layer makes the next action visible without turning the game into a modal walkthrough.
- Validation: Reproduced the 75% carrot / 25% wheat case headlessly and confirmed workers choose carrots when carrot seeds exist and wheat seeds do not. Added a failing regression for the no-empty-plot stall, watched it fail, implemented the alert, and reran the focused simulation suite green.
- Action: Converted the UI control icons from stroke-style SVG paths to 8x8 pixel glyphs and investigated the worker movement complaint.
- Result: Buttons and crop labels now render filled, crisp pixel-art SVG rectangles. The simulation now rejects storage/well placement on a worker tile, and load-time normalization clears any legacy storage/well tile that already overlaps a worker.
- Reasoning: The fresh browser simulation showed workers advancing over real time, but a saved farm could still look frozen if a blocking building had been placed on top of a worker. That created a permanent blocked task that felt like broken movement even though the normal pathing loop was alive.
- Validation: Added failing regression coverage for blocking-building placement on workers and legacy trapped-save recovery, implemented the smallest simulation fix, and reran the focused worker simulation tests green.
- Action: Moved starter storage onto a separate utility tile and closed the remaining placement overwrite path.
- Result: New farms now begin with storage at 7,2, outside the starter plot row, so workers can start seed and crop logistics without any player-built storage. Older saves with the former 6,2 storage position or no storage recover the 7,2 utility bin when that cell is empty or unowned. Building placement now only succeeds on empty owned land; plots, wells, and storage bins stay as-is until the player bulldozes the cell.
- Reasoning: The player should not have to solve basic logistics before the first worker starts working, and accidental direct placement on top of a plot or structure makes the farm feel fragile instead of controlled.
- Validation: Added failing regressions for starter utility storage, legacy storage recovery, and non-overwriting placement, confirmed they failed against the old behavior, implemented the simulation change, and reran the focused simulation tests green.
- Action: Fixed tutorial close-button dismissal.
- Result: Dismissing a first-time tutorial tip now clears the visible tip immediately, and paired tips such as "Open Goals" versus "Buy Seeds" respect their own dismissed state instead of reappearing because a different step in the pair was still unseen.
- Reasoning: The click handler was firing, but some tutorial branches decided whether to show based on the paired next-step tip id rather than the id currently visible. That made the `x` feel unclickable because the same tip re-rendered on the next frame.
- Validation: Added a browser regression that prepares a seed-shortage tutorial, clicks the close button, and confirms no tutorial tip remains in the DOM. The regression failed before the fix with one remaining tip and passed afterward.
- Action: Recolored the UI pixel icons.
- Result: Toolbar, panel, tutorial, crop, and action icons now render inline SVG pixel rectangles with explicit per-icon color palettes instead of inheriting a single monochrome text color.
- Reasoning: The glass UI should remain black and white, but the icon graphics need the same playful color language as the farm objects so buttons are easier to scan and feel less abstract.
- Validation: Added a browser regression that inspects rendered toolbar SVG pixels and confirms multiple non-currentColor fills are present.
- Action: Generated a pixel-art icon concept sheet and scaled the desktop UI up.
- Result: The generated reference is saved at `docs/design/assets/generated-ui-icon-concept.png`. Toolbar buttons, panel tabs, rows, side-panel width, icon sizes, tutorial tips, and small action buttons are slightly larger. The inline pixel icon masks now use dynamic view boxes and more object-like silhouettes inspired by the generated sheet.
- Reasoning: The earlier buttons were compact but hard to read, and the tiny 8x8 icons were too abstract. The generated sheet clarified the desired object language while the code-native SVG pixels keep the app crisp, deterministic, and easy to color.
- Validation: Added browser coverage for scaled toolbar dimensions, icon size, no horizontal overflow, and multi-color pixel fills.
- Action: Added draggable side-panel resizing.
- Result: The side panel now has a thin draggable separator on its playfield edge. Players can widen or narrow the panel, use arrow keys on the focused separator, and keep the chosen width across reloads through a separate UI-preference localStorage key.
- Reasoning: Enlarged UI controls made the panel easier to read, but fixed width still left some text-heavy panels cramped. Width should be player-controlled without changing farm simulation state or the deterministic autosave payload.
- Validation: Added a browser regression that drags the separator, verifies the panel grows without horizontal overflow, and confirms the chosen width survives reload.
- Action: Tightened bottom toolbar button widths.
- Result: Toolbar buttons now use capped, centered grid tracks so they stay compact on wide desktop viewports while preserving the larger icon size and button height from the readability pass.
- Reasoning: The previous equal-width grid made every bottom button stretch to nearly 100px at 1280px wide, which felt heavier than needed for icon-led controls.
- Validation: Extended the browser UI-scale regression with a max button-width assertion and watched it fail before the CSS change, then pass after the compact toolbar update.
- Action: Investigated whether multiple farmers improve efficiency.
- Result: The simulation now treats active planting, watering, and harvesting plot targets as reserved, so later idle workers skip jobs already claimed by another worker and pick a different plot when possible. The LLM playtest metrics now count duplicate active worker targets and flag them as a finding.
- Reasoning: The worker scheduler previously let each idle worker independently choose the nearest eligible plot. Two workers starting from the same tile could both choose the same planting target; one would arrive after the other had already handled it, wasting the second trip and making the second worker feel cosmetic.
- Validation: Added a failing simulation regression that reproduced two workers taking the same planting target, watched it fail, then implemented target reservation and reran it green. Added a failing LLM evaluator test for duplicate active worker targets, then added the metric and finding.
- Action: Reworked the LLM browser playtest harness around player-like control.
- Result: Browser scenarios now reset through fresh localStorage before load, click the visible 4x speed control, open the Goals panel, wait for and click visible tier-claim controls, resize the viewport, and wait real browser time instead of using `window.advanceTime()` or direct browser-side simulation resets. Each scenario report now includes visible text, available player actions, and the player action log that led to the screenshot.
- Reasoning: An LLM playtester needs the same sensory and control surface as a player. Debug APIs are still useful for metrics after observation, and Node-side civ-engine replay remains useful for deterministic debugging, but the browser scenario path should not silently control the game through backdoors.
- Validation: Added a failing harness-contract test that rejected `window.advanceTime()` and browser debug reset control, then made it pass. Added a failing report-rendering test for visible observations and player actions, then made it pass. Reran the LLM playtest and confirmed the generated Markdown contains Visible Observation, availableActions, and playerActionsSincePrevious sections with zero automated findings.
- Action: Added a step-by-step visual LLM playtest loop.
- Result: `npm run playtest:llm:visual-loop` now boots a fresh browser farm, captures a screenshot and visible controls, chooses one player-facing action, executes it, and repeats. The run writes Markdown, JSON, screenshots, and an HTML replay viewer under `output/playwright/llm-visual-loop/`. The default local heuristic uses visible prompts, speed controls, seed buying, crop selling, canvas coordinate clicks, and tier claiming; `FARM_LLM_VISUAL_LOOP_COMMAND` can replace it with an external model/provider that reads the same observation JSON.
- Reasoning: The fixed scenario harness proved screenshots could be collected, but the player/LLM loop needed the actual observe -> decide -> act -> observe rhythm so improvements can be annotated and replayed at the same granularity a person would debug.
- Validation: Added failing contract coverage for the visual-loop command, player-only action boundary, readable action selectors, enough default step budget to claim the first tier, seed-buy selector matching, and non-persistence of external provider command strings. Added failing visible-state coverage so sell-button quantities are not mistaken for crop inventory. Reran the visual loop and confirmed it set 4x, followed tutorial prompts, restocked seeds, opened Goals, clicked Claim Rewards, and reached Tier 2 with 2 workers and no harness findings. External review was unavailable in this local pass; an in-process adversarial review found and fixed the provider-command artifact leak before final verification.
- Action: Ran another visual LLM playtest loop and removed dead-end player actions.
- Result: Empty crop sell buttons and Sell All now disable when there are no crops. Seed and upgrade buy buttons disable when the player cannot afford them. Tutorial tips measure their rendered bounds and clamp back into the visible play area, including canvas-target tips that previously fell below the toolbar. The local visual-loop heuristic now buys the first visible Worker Boots upgrade so upgrade progression is covered before the farm reaches Tier 2.
- Reasoning: Screenshots and available-action logs showed the player-like loop could see controls that looked clickable but would no-op, and the Mix/canvas tutorial cards could be clipped or offscreen. Those states confuse both a human player and the LLM harness because the visible action surface no longer matches meaningful gameplay choices.
- Validation: Added failing browser regressions for empty sell controls, unaffordable purchases, and canvas/panel tutorial bounds, watched them fail, then implemented the UI fixes and reran them green. Added a failing harness contract for visible Worker Boots coverage, then updated the visual-loop heuristic. Reran `npm run playtest:llm:visual-loop` and `npm run playtest:llm`; both produced zero automated findings with fresh screenshots. External review was unavailable in this local pass; an in-process adversarial review of the diff, screenshots, and action logs found no blocker.
- Action: Polished the first-run UI and farm graphics after another screenshot review.
- Result: The canvas now uses a warm-charcoal stage instead of flat black, locked land and owned grass have clearer depth, HUD stats render as compact chips, inventory and upgrade rows have subtle item surfaces, disabled buttons recede more clearly, side-panel tabs use clean icon-only faces while keeping accessible labels, and smaller desktop toolbars switch to icon-plus-key controls instead of truncated labels.
- Reasoning: The previous UI was functional but still felt like a black tooling shell around a small farm. The updated treatment keeps the documented black-and-white glass chrome while giving the farm more toy-diorama depth and removing clipped tab labels from the default panel width.
- Validation: Added failing browser visual regressions for the warm canvas backdrop, HUD stat chips, panel row surfaces, icon-led panel tabs, SVG glyph clickability, and compact toolbar labels, watched them fail, then implemented the CSS, event-target, and Phaser rendering changes and reran the focused suite green. Reran `npm run playtest:llm` and `npm run playtest:llm:visual-loop`; both completed with zero findings and fresh screenshots.
- Action: Expanded the LLM/player harness to touch every visible control surface.
- Result: The scripted browser playtest now tours Inventory, Goals, Crop Mix, Inspect, every toolbar tool, canvas placement/inspection clicks, side-panel dragging/collapse, pause/speed controls, undo/redo, crop-mix range inputs, selling, compact viewport resize, and normal page reload. The step-by-step visual loop action schema now supports drag and range-adjust actions instead of merely listing those controls. Seed-shortage tutorial guidance points directly at visible Inventory seed-buy buttons when the player is already there, and playtest reload setup clears localStorage only for the initial clean boot.
- Reasoning: A player-like LLM harness should not just watch the idle loop and click obvious progression buttons; it needs to exercise every major affordance through the same sensory/control surface as a person. The new reload step exposed that the harness itself was clearing autosave during reload, so the persistence proof needed a one-shot clean boot guard and an evaluator finding for state loss.
- Validation: Added failing contracts for the full surface tour, drag/range visual-loop execution, one-shot storage clearing, and reload-state-loss detection. Added a failing browser regression for seed tips targeting visible Inventory seed buys. Watched the focused failures, implemented the fixes, then reran focused tests, `npm run playtest:llm`, and `npm run playtest:llm:visual-loop` green with zero automated findings. Visual inspection covered the surface-tour, compact, worker-care, post-reload, and final visual-loop screenshots. External review was unavailable in this local pass; an in-process adversarial review of the diff, action logs, reload metrics, and screenshots found no blocker.
- Action: Persisted the selected speed as a UI preference.
- Result: Choosing 2x or 4x now writes a separate `farm-speed-v1` localStorage preference and reloads restore the HUD plus active toolbar state to that speed. The deterministic farm autosave payload still does not include UI speed.
- Reasoning: The continued LLM playtest loop showed that the farm state restored after reload, but the speed dropped back to 1x. In an idle game, the selected speed is part of the player's working setup, so forgetting it makes reload feel like a partial reset even when the simulation state is intact.
- Validation: Added a failing browser regression that selected 4x, reloaded, and saw the HUD return to 1x. Fixed speed persistence through a separate preference key and reran the focused regression green. Reran `npm run playtest:llm`, `npm run playtest:llm:visual-loop`, `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build`; all passed, with only the existing Vite large-chunk warning. External review was unavailable in this local pass; an in-process adversarial review verified that speed stays out of `farm.autosave.v1` and is stored only in the separate UI preference key.
- Action: Cleared side-panel tutorial cards away from the controls they describe.
- Result: Tutorial cards for seed restocking, sell controls, and side-panel tabs now render beside the side panel when there is room, while canvas and toolbar tips still clamp inside the playable area above the toolbar.
- Reasoning: The continued visual pass showed the "Buy Seeds" and "Open Goals" cards could cover Inventory buttons while asking the player or LLM harness to click nearby controls. Guidance that blocks the action creates unnecessary hesitation even when the underlying button is still technically clickable.
- Validation: Added failing browser regressions proving seed guidance overlapped wheat/tomato buy buttons and the Goals tab tip overlapped sell buttons, then fixed side-panel tip placement and reran the tutorial suite green. Reran `npm run playtest:llm` and `npm run playtest:llm:visual-loop`; both produced zero automated findings. Visual inspection covered post-reload, worker-care, visual-loop step 15, and final screenshots. External review was unavailable in this local pass; an in-process adversarial review checked that tips stay clear of side-panel controls and remain above the toolbar.
- Action: Named the Crop Mix slider controls for player-like reports.
- Result: Crop Mix range inputs now expose crop-specific `aria-label` and `title` text such as `Set Wheat crop mix`, so browser playtest reports and visual-loop replay prompts no longer show anonymous `input` actions for those sliders.
- Reasoning: The LLM can only act like a player if the visible action list uses the same meanings a human reads from the panel. Anonymous range inputs made crop-mix tuning harder to audit even though the slider rows were visually clear.
- Validation: Added a failing browser visual-polish regression for Crop Mix slider labels, watched it fail with empty labels, then added the labels and reran the focused suite green. Reran `npm run playtest:llm` and `npm run playtest:llm:visual-loop`; both produced zero findings, and the generated reports now list `Set Carrot crop mix` and `Set Wheat crop mix` actions. Visual inspection covered the Crop Mix screenshots after the report regeneration.
- Action: Added camera pan and zoom to the scripted LLM/player surface tour.
- Result: The browser playtest now holds `ArrowRight` to pan the Phaser camera and wheels over the canvas to zoom before capturing the full surface-tour screenshot.
- Reasoning: The game design names pan and zoom as player controls, but the harness covered tool clicks, panels, ranges, drag resizing, reload, and hotkeys without actually moving the camera. A player-like LLM loop needs to exercise spatial navigation too.
- Validation: Added a failing harness contract for camera pan/zoom coverage, watched it fail on the missing helpers, then implemented held-key and canvas-wheel helpers and reran the focused contract green. Reran `npm run playtest:llm`; it produced zero findings, recorded the camera actions in `player-surface-tour`, and the screenshot confirmed the shifted/zoomed farm remained readable with the HUD, panel, and toolbar usable.
- Action: Added camera pan and zoom actions to the step-by-step visual LLM loop.
- Result: The visual-loop action schema now accepts `wheel` decisions and bounded `durationMs` key holds. The default local visual player holds `ArrowRight` and wheels over the canvas before continuing through Goals, Worker Boots, selling, plot painting, and tier claiming.
- Reasoning: The visual loop was the closest harness to a real LLM player, but it could not decide to use the full camera control surface it advertised in the game. Matching keyboard-hold and wheel input makes the observe-decide-act loop cover spatial navigation, not just buttons and panels.
- Validation: Added failing contract expectations for the larger default step budget, `wheel` schema support, wheel execution, held-key duration, and camera-zoom rationale. Watched the focused contract fail, implemented normalization and execution support, then reran the focused contract green. Reran `npm run playtest:llm:visual-loop`; it completed 18 steps with zero findings, recorded `press` and `wheel` decisions, and screenshots confirmed the zoomed/panned farm, HUD, toolbar, tutorial card, and side panel remained readable.
- Action: Fixed stale Inventory crop rows during fast browser screenshots.
- Result: The Inventory panel now bypasses its 250ms throttle when visible inventory state changes, keeping crop rows, Sell All availability, HUD storage, and tutorial guidance aligned on the next frame.
- Reasoning: The compact LLM screenshot showed `STORAGE 1/30` and a Sell Crops tutorial while all visible crop rows still read zero. That split-second mismatch is easy for an LLM or player to interpret as hidden inventory.
- Validation: Added a failing browser regression that forced an Inventory render, advanced the farm, then sampled the next animation frame; it failed with HUD storage at 8 and visible crop rows at 0. Added an Inventory state signature to the panel render guard and reran the focused regression green. Reran `npm run playtest:llm`; it produced zero findings, and compact plus worker-care screenshots showed storage totals matching visible crop rows.
- Action: Gave the step-by-step visual LLM loop enough default budget to finish the first tier claim.
- Result: The default visual loop now allows 20 actions before the final screenshot.
- Reasoning: The post-fix visual loop used its 18th action to reach the ready Tier 2 claim state, then stopped with the claim button visibly waiting. The harness should not end one player click before its own first-tier success path.
- Validation: Changed the visual-loop budget contract first and watched it fail against the 18-step default, then raised the default to 20 before rerunning the focused contract and live visual loop.
