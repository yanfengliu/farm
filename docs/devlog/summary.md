# Devlog Summary

- 2026-07-17: Fixed the expand-panel arrow swallowing real clicks - the inverse follow-up to the collapse-reopen fix. The toggle icon was rebuilt via unconditional per-frame `innerHTML` (the one uncached element in the UI), so a human-speed press had its target destroyed mid-hold and the browser dropped the click; instant automated clicks slipped between frames, which is why the existing contract stayed green. The icon now rewrites only on state flips, and a new contract presses at human speed and requires the icon to survive the hold. Gates 64 of 64 twice consecutively.

- 2026-07-17: Fixed the player-reported unreopenable collapsed panel. The toggle worked mechanically, but collapsing hid every tab and left one unlabeled arrow (the redo glyph) that teleported ~300px to the far corner — an unfindable affordance honestly reported as broken. The collapsed strip now keeps all eight tab icons visible in a vertical column; any tab click reopens the panel on that tab, and inspecting the farm while collapsed reopens it with the result. Red-first contracts pin tab visibility and hit-testability while collapsed, both reopen paths, and the unchanged toggle. The gate runs also flushed the last unswept budget class — a `beforeAll` that boots the app against vitest's default 10s hook timeout — now aligned with the suite's contention-aware budgets. Gates 64 of 64 twice consecutively.
- 2026-07-17: Fixed the player-reported panel flicker and inspection hijack with one root-cause change each. Panels now morph their DOM instead of rewriting it (`domMorph.ts`, ~70 lines): unchanged controls keep identity across live-data re-renders — pinned by browser contracts marking real buttons through a minute of farm churn — so hover no longer flickers on every heartbeat. Inspect selection became explicit and sticky: clicking a farmhand selects him by id with corner brackets and follows him; clicking a cell inspects the cell, which structurally can no longer show a passing worker; a dedicated Farmhands tab lists every hand with portrait, task, and cargo, with roster rows toggling the same selection. The morph also exposed a latent Farm Notes bug that had passed by focus-destruction accident — Enter during a held box drag activated the still-focused Box button and silently cancelled the drag; Enter is now inert mid-drag regardless of focus. `goalsMarkup` extracted to its own module to hold the 500-line ceiling. Gates 63 of 63 twice consecutively.
- 2026-07-17: Ringed the legal camera world with a deterministic border woodland on player direction — ~230 hash-jittered trees with density thinning from the world edge, side-weighted species pools, and per-candidate rejection against the framed view (26px breathing), the environment bounds, and the meandering creek. Red-first contract pins counts on all four sides, clearances, variety, jitter, and determinism; woodland trees flow through the existing draw lanes and are Farm Notes-pickable automatically. Two old contracts adapted honestly: the botany-grammar helper looped instead of spreading past the argument limit (the grammar itself validates all ~230 trees), and the recenter rule now states its real intent — no half-tree straddling the default crop edge from either side — instead of encoding the sixteen-tree world. The run also flushed out a latent autosave-throttle race in the work-effects purity contract, fixed by settling the save before snapshotting. Gates pass 59 of 59 twice consecutively; the default framed view is pixel-familiar and the zoomed-out world reads as forest on every side.
- 2026-07-17: Acted on the third player-exported Farm Note, which proved the morning's fix and exposed its residue in one gesture: the note at wild cell (1,8) correctly captured the new `vignette:bee-skeps` target and label — and the player still asked "What is this?", because "skep" is archaic vocabulary and the label only helps players who know the word. The beehives are now labelled "Straw Beehives" (semantic id unchanged), every southern vignette carries a one-line description, and Inspect — the tool actually built for this question — introduces wild meadow stories by name with their description, "buying clears it" context, and intact purchase guidance, while plain locked land inspects as before. Red-first unit contracts plus a live-browser capture of the finished card; gates pass 58 of 58 files.
- 2026-07-17: Acted on the second player-exported Farm Note: "What is this?" pinned at wild cell (7,7) — the hay bales, which were three stacked solid rectangles the organic-art pass never reached. The note evidenced two defects at once, both fixed red-first. Art: all three southern vignettes now satisfy the stone-grammar raster contract (the old hay bales measured a repeated-row-width run of 15; the recut round coiled bales, ridged dome bee skeps with drifting bees, and tattered-hem scarecrow each pass varied-width, bounded-occupancy, and broken-shadow rules), verified readable in zoomed browser captures. Naming: `SOUTHERN_MEADOW_VIGNETTES` is one authored list feeding both renderer and semantic picking, so a note at (7,7) now drafts as "Hay Bales" — the player's own gesture would have answered their question — while purchased cells fall back to tile labels because stories yield to owned land; notes on farmhands now use their authored names. Gates pass 57 of 57 files.
- 2026-07-17: Shipped the living-farmhands and storybook-chrome enrichment from a committed spec. The four farmhands are named residents (Fern, Alder, Poppy, Rowan — content-only projection, no save change), and inspecting one shows a framed pixel portrait rendered through a new two-method `PixelPainter` interface that both Phaser Graphics and a DOM canvas adapter satisfy — the portrait is the scene's own sprite, never a copy, enabled by the discovery that every art module's Phaser import is type-only. Work is celebrated presentation-only in the effects lane: planting dust, watering droplets, and harvest sparkles spawn from lifetime-stat diffs at the matching worker, capped, animated on presentation time, and provably absent from saves — a browser contract holds state text and save bytes byte-identical across a second of live effect frames on a paused farm. Chrome accents (heading flourish, chip frames, Coins pulse) are layout-neutral with panel overflow measured 0 at both supported sizes. The gate run surfaced rotating browser timeouts; a clean-checkout baseline proved the slowest tests already ran 10-12s solo (11.8s on `main` vs 9.8s enriched — the feature slowed nothing), so every inline 15s browser budget was swept to 30s. Gates green 56/56 three consecutive runs; a bounded 24-step visual loop ran with zero console/page errors.

- 2026-07-16: Decided and shipped the paused-command UX uncovered by the canvas-placement work. Commands queue into the engine world and apply only during a step, and a paused farm never steps on its own, so every paused player command — placement, purchase, undo — looked dead and then burst all at once on resume. Headless verification confirmed deferred-not-dropped and whole-queue bursts. Decision: a paused farm applies player edits immediately by stepping exactly one recorded tick per command at the single UI-bridge choke point (the engine exposes no public command-only flush), keeping edits visible and deterministic while autonomous work stays halted; the ~100ms game-time cost per deliberate edit is documented in the design doc. `pausedCommands.test.mjs` pins purchase-lands-while-paused, one-tick-per-edit, no free-running, and paused undo of a paint — red-first, green three times, mutation-proved. Its first draft exposed a design fact now written down: seed purchases and crop sales are deliberately outside the undo domain. Gates pass 52 of 52 files.
- 2026-07-16: Closed the last two blind-spot lanes: canvas tool placement and the loop's save-reload restore scope. `canvasToolPlacement.test.mjs` proves real clicks at calibrated screen coordinates place a well, place storage (HUD capacity follows), buy land (HUD coins follow), and bulldoze — Inspect-verifying every target cell before the real tool clicks it, mutation-proved by disconnecting one tool case. Its first draft exposed the session's key mechanic: `submitFarmCommand` queues into the civ-engine world and applies only during a step, so a paused farm silently defers every player command with no UI signal — the test now plays unpaused and the paused-UX question is spawned as its own product decision. The recursive loop gained `reload-check.mjs`: after bundle export and replay self-check (reload destroys the in-page recorder), it reloads like a returning player and compares farm identities against the stored autosave — identity exact, progress monotonic, game-declared refusal its own violation — raising critical `autosave-reload-lost-state` (the class resurrects the orphaned P1 oracle) and declaring the lane in the pass's discovery scope. Building its negative proof caught the lane's own false-positive bug before it shipped: the loop's per-navigation storage clear wiped the autosave the check was proving; the clear is now sessionStorage-guarded to once per context and contract-pinned. Acceptance both ways on real runs: seeded schema-drift raises `save-refused, tick-regressed, progress-regressed`; clean restores tick 41 → 45 with zero violations.
- 2026-07-16: Closed yesterday's named open gap — browser-level migration of legacy-but-valid saves — and killed the suite's two intermittent browser flakes. `tests/browser/legacySaveMigration.test.mjs` boots a genuinely pre-Village-Harvest save (no `wildlife`, `community`, `upgrades`, optional counters, or `pumpkin` entries, including inside a stripped undo snapshot) through the real `main.ts`/Phaser/autosave path and proves the save loads rather than being refused, the played tick survives, every additive migration lands and reaches both the text projection and a painted canvas, and autosave writes back a current-schema save; commenting out the wildlife migration fails it with `Cannot read properties of undefined (reading 'ducks')` — the unit-valid-but-breaks-downstream class the lane exists for. The `speedPreference` flake was a test-logic bug: it waited on a bare `#panel-content h2` selector the previous panel already satisfied, then read the stale heading before the next Phaser frame swapped Goals in. The `annotations` flake took three instrumented probes: not budget starvation, not per-frame churn — after a save the stale draft card legally persists one frame (overlay refreshes synchronously, panel on the next frame), so around a second draft's open the panel rebuilds stale-card → aim-card → draft-card and each rebuild detaches the Cancel node under `page.click`. Cleanup moved to the documented double-`Escape` keyboard path, which is synchronous and cannot race a rebuild; no app code changed for either flake. Gates pass 49 of 49 files twice consecutively.
- 2026-07-15: Broadened discovery past the recursive pass's declared scope and closed the gap it exposed: nothing in the repo had ever loaded a save in a browser. The visual loop clears `localStorage` on every navigation, so it exercises the write half of persistence and never the read half; the good unit coverage runs only against an in-memory storage double; and the seven browser tests that reload all assert a DOM preference or a note, never farm state. The lane had regressed rather than never existing — a live P1 `autosave-reload-lost-state` evaluator still sits in `scripts/llm-playtest/evaluate.mjs` diffing scenarios nothing has produced since that harness became a shim. Underneath, `loadSavedFarmState()` collapsed "no save", "parse failure", and "validation rejected" into one null that `main.ts` turned into a fresh farm with no signal, and the ~1s autosave then overwrote the rejected save, destroying it. `loadFarmSave()` now reports `loaded`/`empty`/`unreadable`, and for `unreadable` the farm declines to destroy what it cannot repair: autosave latches off, the stored bytes are left untouched, and a standing HUD warning names Reset as the way out; Reset drops the save and resumes autosave, making it the player's consent. A first copy-aside design was refused by two independent adversarial reviewers and abandoned on the code — a copy needs ~2x storage while the overwrite that follows needs none, so it failed precisely on the large saves worth protecting (history is ~97% of a payload, projecting to ~9 MB at 400 tiles against a 5 MB quota); a second copy silently clobbered the first, and nothing ever read a copy back, so "kept for recovery" promised a path that did not exist. Both contracts pass on unfixed code for different reasons, so both are mutation-proved: forcing `isFarmState` false fails the reload contract with `expected 3 to be greater than or equal to 603`, and removing the autosave block fails the no-overwrite contract with the destruction in the assertion output. Required browser evidence caught the notice truncating at 1024x720, which generalized: the pre-existing autosave-failure (101 chars) and reset-failure (85) warnings truncated too, losing their actionable halves, because the existing assertions read `textContent` and CSS ellipsis is invisible to it. All three messages are now at or under 53 characters with rendered-width contracts at both desktop sizes. Gates pass 48 files. Browser-level migration of legacy-but-valid saves stays an open gap.
- 2026-07-15: Closed out the visual-loop screenshot guard after two independent adversarial reviewers refused the inherited version. Its detection ANDed the run-length trips behind a `>= 0.005` coverage floor, so a pure-black band spanning the entire frame width and a full-height black column both read clean — the guard missed the corruption class it was named for, and the floor sat 5,120 pixels above a measured baseline of exactly zero. Coverage and run-length trips are now independent at 0.002 and 0.25. Throwing on exhaustion was also at the wrong altitude: the final observation is captured inside a `try` with only a `finally`, so one bad frame discarded a completed run's report, bundle, and ledger row. Capture now keeps the least-corrupt frame, marks it degraded, and raises a `screenshot-compositor-degraded` finding. Retrying the whole observe-and-shoot attempt restores the documented DOM/screenshot pairing, so the existing sync contract passes unmodified; capture metadata is attached only to frames that retried, removing ~48 kB of write-only JSON per run. Gates pass 47 files, and a real 8-step loop wrote 9 of 9 clean screenshots with no errors or degraded frames.
- 2026-07-15: Restored `npm test` as a trustworthy gate. The suite was red on a clean checkout of `main` — 14 files failed / 380 of 394 tests at commit `a0b6a6b` — and every failure was an identical `Test timed out in 15000ms` rather than an assertion, so the repo's most authoritative gate could not tell a real regression from runner noise. Sixteen `tests/browser` suites each launch their own Chromium and Vite server while setting their own inline 15s budgets, and with no Vitest config present the runner defaulted to one fork per core and started ~31 of them at once. A new `vitest.config.ts` caps `poolOptions.forks.maxForks` at `max(2, min(6, cores - 1))`, taking the same suite at the same commit from 14 files failed to 46 of 46 passing across repeated runs, for ~45-49s against the ~40s of the previously-failing run. No timeout was raised and no test weakened; `tests/browser/testRunnerConcurrency.test.mjs` pins the cap against regrowth and fails on a mutation that restores the uncapped default.
- 2026-07-14: Replaced the remaining block-like vegetation, crop, waterside, rock, paver, path, and chimney-smoke composites with deterministic organic pixel silhouettes. Shared asymmetric leaf masks, visible stems and branch structure, tapered carrot blades and roots, bent wheat stalks with separated grain, stepped fruit, notched lily pads, tapered bank plants, shouldered mineral faces, broken shadows, and drifting smoke preserve the cozy palette without opaque green or grey placeholders. Exact scenery collision contracts move the north hedge, western elder, garden sign, east understory, and southeast willow onto natural clear ground; creek communities vary species clusters without a rigid alternation.
- 2026-07-14: Organic-art verification is green across 45 test files / 394 tests, typecheck, lint, and production build. Fail-first raster contracts cover every crop stage and wet state, authored hedge seed, botany and waterside species, every meadow/cottage/path stone variant, all 20 smoke phases, production routing, and scenery collisions. Fresh headless evidence at 1280x800, 1024x720, and full expansion has no plant/stone/smoke rectangles, clipping, UI overlap, or text-fit regression after an independent visual refuter found and rechecked the last blocky smoke puffs. The build retains only the established Phaser chunk-size advisory.
- 2026-07-13: Player-annotated hedgerow verification passes 38 test files / 327 tests, typecheck, lint, and production build. The build retains only the established Phaser chunk-size advisory.
- 2026-07-13: Acted on the first player-exported Farm Notes Box bundle. Its evidence crop and exact world rectangle isolated the east `Wild Hedgerow`; the authored seed had collapsed the old modulo-based variation into five equal-width, equal-step rectangles with the same highlight and flower rhythm. Fail-first contracts now pin the production placement, deterministic width/crown/baseline/spacing variety, compact rounded lobes, varied highlight counts, actual renderer fill widths, a non-strip footprint, and four pixels of breathing room inside the default recenter view. The renderer uses layered pixel clusters, segmented shadows, ground sprouts, sparse blossoms, and occasional shoots, while semantic annotation picking returns `hedgerow:east` at the original note center instead of `Meadow / 15,5`. Headless review covered 1280x800, 1024x720, and a settled replay of the attachment's exact 2560x1305 camera; the complete thicket remains visible at both supported sizes.

- 2026-07-13: Closed the low-severity recursive-loop candidate `coverage-gap-data-sell-tomato` uncovered while proving the freeform Box follow-up. The Box lifecycle stayed semantically identical through its first 49 decisions; later harvest timing let the local player satisfy its Tier 4 stop gate before exercising a repeatedly visible Tomato sale. A red completion test reproduced `stop` at low storage, the fix now requires the full Carrot/Wheat/Tomato/Pumpkin single-sale tour before a clean terminal stop while preserving the existing rule that late-game sales wait for guidance, storage pressure, or low coins, and focused coverage passes 87 tests. Final run `farm-visual-loop-2026-07-14T04-37-22-653Z` completed 156 decisions with zero findings, failed actions, console errors, or page errors; replay verification was strong with one checked and zero skipped/diverged segments, and `comparison.resolved` mechanically names `coverage-gap-data-sell-tomato`.
- 2026-07-13: Corrected the Farm Notes Box activation flow after direct player feedback. Rectangle geometry was already continuous-pixel and freeform, but clicking Box while idle changed only the mode; the next drag therefore fell through to the selected farm tool and looked like a one-cell box. Point/Box controls now arm aiming immediately, Box-mode `Enter` gives drag guidance instead of creating a fixed centered region, the live overlay reports width and height, and draft/list labels lead with area dimensions while retaining the center target only as context. The browser regression starts idle, clicks Box once, reverse-drags across non-grid-aligned coordinates, and proves the exact large region persists without farm-state or history mutation.
- 2026-07-13: Added first-class rectangular Farm Notes without breaking version-1 point records. Point/Box mode selection, reverse and off-canvas drag normalization, tiny-drag rejection, native pointer capture, centered keyboard boxes, draft-time and saved world outlines, camera-aware View/reload projection, aspect-preserving evidence crops, cross-space persistence validation, debug context, and the LLM player's real box gesture now form one tested annotation contract. Three independent adversarial reviews reproduced and closed camera-tampering, edge-preview distortion, pointer-release, pause-ownership, reload-visual, mouse-cleanup, and partial-success player-state gaps.
- 2026-07-13: Bounding-box verification is green across 37 test files / 321 tests, typecheck, lint, and production build. Headless browser evidence at 1280x800, 1024x720, and DPR2 shows clean pixel outlines, undistorted previews, retained draft regions, and fitted controls. The expanded keyboard audit checked 349 keydown/keyup events in each real draft/edit textarea and found one additional implicit Phaser Shift-keyup capture; after removing it, no unintended key is prevented or routed to gameplay, with only `Escape` cancel and `Ctrl`/`Cmd` + `Enter` save intentionally consumed. Initial recursive wrapper `farm-recursive-2026-07-14T02-33-39-313Z` honestly failed because the prior 5175 listener had ended; after relaunch, `farm-recursive-2026-07-14T02-42-48-785Z` completed all 160 bounded decisions with zero findings, action failures, console errors, or page errors and visibly exercised the full Box lifecycle. Separate replay inspection of ticks 5824-5887 reports `selfCheckStrongOk: true`; this proves only the declared deterministic surface, with broader quality not evaluated.
- 2026-07-13: Fixed Farm Notes dropping `W`, `A`, `S`, and `D` during real keyboard typing. Phaser had registered those camera keys with browser-default capture, so its global listener prevented textarea insertion even though focused controls already blocked camera movement. A dedicated browser contract now real-types mixed-case WASD through draft and edit, verifies the exact message through debug export, local storage, and reload, holds `D` in the editor to prove the pin stays still, and holds all four movement keys on the canvas to prove directional camera motion. A mutation check that restored Phaser capture made the contract fail on the exact mangled draft, then restoring the fix made it green again. The focused contract stayed green across three repeated runs and the final gates pass 35 files / 304 tests, typecheck, lint, and production build.
- 2026-07-13: Final post-hardening visual run `farm-visual-loop-2026-07-14T00-05-11-509Z` agent-stopped after completing 146 of at most 160 decisions with no console, page, engine, improvement, or open coverage findings. Retained ticks 5568-5632 strongly replay-checked one command segment with zero skips or state, event, and execution divergences. Clean-revision wrapper `farm-recursive-2026-07-14T00-05-08-979Z` reports `no-fix-candidate` only for `local-heuristic-full-surface-160-step`, with broader quality `not-evaluated` and `broaden-discovery` next. Its self-contained JSON/Markdown/HTML/bundle plus 147-screenshot packet has zero missing, outside-packet, stale-canonical, or byte-mismatched image references; the replay bundle also matches canonical bytes. Full gates pass 34 files / 303 tests, typecheck, lint, and production build.
- 2026-07-13: Final Farm Notes and recursive-loop hardening closes four adversarially reproduced proof gaps: a draft now blocks all gameplay clicks and global shortcuts until save/cancel, the deterministic player derives curriculum state only from successful browser executions and performs Undo/Redo before any intervening command, capped displayed findings retain an uncapped open-id comparison set, and per-pass archives rewrite their bundle/screenshot references into self-contained JSON/Markdown/HTML plus `steps/` evidence. Focused red-to-green browser and filesystem contracts cover each failure. Codex authentication is now valid, but external Codex and Claude review export is prohibited by the private-repository policy, so independent in-process review remains the claimed review boundary.
- 2026-07-13: Final Farm Notes verification is green across 33 test files / 292 tests, typecheck, lint, and production build. Headless visual review refreshed aiming, populated draft, and pinned-note evidence at 1280x800 and 1024x720 with no overlap, horizontal overflow, clipped action labels, or unnatural annotation chrome. Targeted visual-loop run `farm-visual-loop-2026-07-13T21-05-26-586Z` used the local screenshot/visible-control heuristic against localhost and successfully executed Note -> canvas target -> labelled bounded text entry -> Pin -> Notes -> world marker as its first six actions, with all 24 actions successful and no console, page, or engine errors. It attached to the existing server, so replay verification is null, and it honestly ended at its 24-step cap on actionable Select Plot guidance plus three low later-curriculum coverage gaps; this targeted proof is not a clean recursive pass.
- 2026-07-13: Adversarially hardened Farm Notes after three independent in-process reviews. Deep nested validation now drops forged capture state, missing camera data, and non-PNG preview payloads before rendering; all interpolated capture metadata stays escaped. Keyboard aiming captures the canvas center, edge-clamped preview crops place their crosshair on the real click, unsaved draft/edit text survives panel changes, reset clears focused editors, existing pins stop intercepting new targets, empty comments announce an inline error, copy/export/delete announce outcomes, and deletion requires a second explicit confirmation. The LLM provider now rejects action kinds incompatible with each observed control and coverage counts only successful execution. External Codex and Claude CLI review attempts both returned HTTP 401, so no external approval is claimed.
- 2026-07-13: Added Farm Notes as a presentation/debug layer independent of deterministic farm state. Note mode captures exact client/canvas/normalized/world coordinates, semantic scenery or actor targets, camera/viewport state, click-time state text, a history-free frozen FarmState, and a small PNG crop; saved records use stable numbered pins, a camera-restoring Notes list, edit/copy/export/delete controls, separate bounded `farm.annotations.v1` persistence, past-farm reset context, and cloned/serialized `__farmDebug` access. Browser contracts cover persistence, hostile HTML, non-mutation, pause ownership, reset context, camera projection/restore, coordinate precision, and 1280x800 plus 1024x720 fit.
- 2026-07-13: Extended the canonical LLM visual player with a validated bounded `type` action for visible textareas and a deterministic Note -> canvas target -> comment -> pin -> Notes tab -> world-marker curriculum. A 24-step live smoke executed all four creation actions successfully. Longer runs exposed and then regression-pinned a tutorial card intercepting guided canvas clicks; the proof rerun resolved both high-severity action-failure findings. The latest 160-step full-surface run had no browser/action/engine failure but honestly remains capped on actionable Tier 3 guidance, with one medium curriculum-budget finding and low unexercised annotation-control gaps; it is not claimed as a clean recursive pass.
- 2026-07-13: Halved duck travel cadence across both neighboring creek swims and long shelter journeys, staggering deterministic movement updates by duck ID so Pip and Mallow no longer hurry or step in lockstep. The focused wildlife contract first reproduced the old 20/10 progress over ten ticks and now pins the slower 10/5 pace without changing hunger, fishing, sleep, save, or replay state. A paused headless localhost measurement advanced exactly 20 ticks after reset and observed only 14/10 progress after the ducks' authored initial rests; `output/playwright/duck-speed/slower-ducks-1280x800.png` records the clean frame. Final gates pass 28 test files / 271 tests, typecheck, lint, and production build.
- 2026-07-13: Replaced the farm's repeated canopy stamp, rigid hedge train, and detached foreground leaf blocks with a deterministic living hedgerow. Sixteen elder, hazel, birch, apple, and willow trees now use distinct silhouettes and grounded trunks; named duck shelters no longer depend on grove-array positions; permanent tree and plant pixel bounds stay clear of expansion, bridge, garden, and default recenter crops. Sparse ferns, foxgloves, lavender, berry shrubs, mushrooms, cottage window boxes and two planted beds, alternating sedge/cattail/iris creek communities, and low clover on empty owned land add plant communities while functional tiles and purchased land remain readable. Headless visual review covered starter farms at 1280x800 and 1024x720 plus a fully expanded 1280x800 farm, and independent visual/content reviewers found no remaining substantive weirdness after the bounds hardening pass.
- 2026-07-13: Living-hedgerow verification is green across 28 test files / 270 tests, typecheck, lint, and production build. Final recursive run `farm-visual-loop-2026-07-13T19-17-25-242Z` agent-stopped at decision 127/140 after reaching Tier 4, with no console/page/engine errors and no findings; its partial retained ticks 5568-5632 strongly replay-checked one command segment with zero skips or state/event/execution divergences. Wrapper `farm-recursive-2026-07-13T19-17-22-875Z` records `no-fix-candidate` only for `local-heuristic-full-surface-140-step`, with broader quality `not-evaluated` and `broaden-discovery` next.
- 2026-07-13: Turned Pip and Mallow from a decorative ping-pong pair into saved deterministic wildlife. They now grow hungry and tired, reserve distinct renewable fish, swim and feed, seek shared grove shelters, sleep under tree canopies, wake rested, and expose their needs and activity through text/debug state. The creek's mystery shapes are now clearly varied lily pads with optional multicolor blossoms, irregular hashed positions, notches, and companion leaves. Legacy local saves gain the starter ecology while pre-ecology replay snapshots remain exact, and strict validation rejects incoherent duck activities or fish reservations.
- 2026-07-13: Ecology verification is green across 26 test files / 261 tests, typecheck, lint, and production build. Final recursive run `farm-visual-loop-2026-07-13T18-32-10-936Z` consumed the 140-decision ceiling at Tier 4 with no console/page/engine errors and no findings, then strongly replay-checked one command segment over ticks 6336-6400 with zero skips or state/event/execution divergences. Wrapper `farm-recursive-2026-07-13T18-32-08-669Z` records `no-fix-candidate` only for the declared `local-heuristic-full-surface-140-step` scope, with broader quality `not-evaluated` and `broaden-discovery` next; external Codex and Claude reviewers remained unavailable with HTTP 401, so no external approval is claimed.
- 2026-07-13: Deepened the code-native art direction into a layered storybook farm: shared scenery geometry now keeps the camera, full-height creek, bridge, cottage garden, laundry, groves, and farm frame aligned; water, scenery, actors, overstory, ambience, and selection have explicit depth lanes; ducks, butterflies, smoke, sun motes, richer farmhand props, and southern hay/bee/scarecrow-and-crow vignettes make starter and mature farms feel inhabited while wild-cell stories cleanly yield to purchased land. Tier 2 produce crates, Tier 3 bunting, and a Tier 4 harvest stall make progression visible outside the buyable grid. Adversarial review moved permanent canopy clear of every fully expanded cell, froze actor poses with simulation pause, separated four converged workers, replaced visible modulo teleports with continuous routes, and made frame-delta easing partition-independent. Headless starter and mature-farm screenshots at 1280x800 and 1024x720 plus palette, crop-silhouette, motion, full-expansion, panning, and compact UI contracts pin the result.
- 2026-07-13: Post-hardening recursive re-drive `farm-visual-loop-2026-07-13T09-43-07-439Z` consumed the full 140-decision ceiling, reached Tier 4 Harvest Hearth with four workers, reported no page/console/engine errors and no findings, and strongly replay-verified one retained command segment over ticks 6080-6144 with zero skips or state/event/execution divergences. Wrapper pass `farm-recursive-2026-07-13T09-43-05-328Z` records `no-fix-candidate` only for `local-heuristic-full-surface-140-step`, `not-evaluated` broader status, and `broaden-discovery` next action; the visual loop ended at its ceiling rather than agent-stopping, so this is bounded regression evidence rather than a broad quality claim. Final gates pass 25 files / 244 tests, typecheck, lint, and production build.
- 2026-07-13: Recursive visual verification `farm-visual-loop-2026-07-13T08-52-43-905Z` reached Tier 4 Harvest Hearth and agent-stopped at decision 128/140 with four workers, no console/page/engine errors, and no findings. Because the pass attached to the already-running 5175 server, its packet honestly left `replayVerification` null; the separate replay inspector closed that boundary with `selfCheckStrongOk: true` over the retained ticks 5312-5376. The pass manifest reports `no-fix-candidate` only for `local-heuristic-full-surface-140-step`; an independent final-screenshot/state audit also found no actionable visual candidate.
- 2026-07-12: Added the Village Harvest content and art pass: deterministic neighbor request baskets create a retain-versus-sell planning loop, three completed deliveries plus ten harvested tomatoes unlock Tier 4 pumpkins and a fourth farmhand, and the farm now lives in a layered sunlit meadow with a creek, bridge, cottage, richer crops and workers, warm harvest chrome, migration coverage, and browser visual contracts.
- 2026-07-12: Final post-review recursive re-drive `farm-visual-loop-2026-07-13T08-08-06-549Z` reached Tier 4 Harvest Hearth and agent-stopped at decision 134/140 after exercising all 45 offered semantic controls, including both Tomato and Pumpkin Crop Mix inputs, every crop sale, both late seed purchases, and both worker upgrades. The pass reported no page, console, or engine errors, no findings, and `no-fix-candidate`; its bounded 64-tick replay window (ticks 5952-6016) checked one command segment with no skips or state, event, or execution divergences, and comparison added, persisted, and resolved no findings. The manifest records the dirty source revision and declares `local-heuristic-full-surface-140-step` scope, `not-evaluated` broader status, and `broaden-discovery` next action: this proves the deterministic declared scope is quiet, not that every possible play path is complete.
- 2026-07-12: Adversarially hardened the Village Harvest pass with bounded history, fail-safe persistence warnings, connected-map and worker-path validation, safe worker spawning, conservative request-counter migration, stable Crop Mix editing, bounded and recenterable camera controls, pause-independent ambient animation, and expansion-safe landmarks. The full browser and simulation suite now covers 233 tests; external CLI reviewers were unavailable with HTTP 401, so parallel in-process reviewers and direct code verification supplied the review evidence.
- 2026-07-12: The first 120-decision recursive shift reached its final screenshot but correctly failed at replay export when a browser-lifetime `SessionRecorder` exceeded Playwright's string transport limit. Farm now uses bounded 64-tick evidence windows, prefers a recent command-bearing window over a vacuous idle suffix, records whether that evidence covers the full run, refuses to globally verify findings with partial replay evidence, and escapes active-request stalls by opening Inventory and selling only surplus above visible basket reserves.
- 2026-07-12: Recursive rerun `farm-visual-loop-2026-07-13T06-24-21-415Z` proved the replay repair with one checked segment, no skips or divergences, and resolved the Wheat-sell coverage gap, but remained proposal-only after repeated pressure sales and milestone-first tomato restocking delayed a carrot-deficit basket. The follow-up stops pressure sales with two free slots, gives a missing request crop first claim on zero-stock seed purchases, and clears request priority after Unpin.
- 2026-07-12: Recursive rerun `farm-visual-loop-2026-07-13T06-36-31-132Z` again replay-checked one segment without divergence and reached Tier 4, but unconditional visible Pin actions had completed five deliveries and started a sixth. The bounded curriculum now demonstrates Unpin once, retains the replacement, and stops auto-pinning after the three requests required for Harvest Hearth.
- 2026-07-12: Long recursive rerun `farm-visual-loop-2026-07-13T06-42-30-514Z` completed in 326 seconds with bounded replay still strong, resolved Unpin/Tomato-sell/Pumpkin-seed gaps, and exposed 65 identical waits after the three-request curriculum ended on visible seed guidance. Seed shortages now open Inventory from any inactive panel, not only while a request is pending.
- 2026-07-12: Hardened the playable base before the next content increment. Farm now rejects malformed version-1 saves at the persistence boundary, quotes exact seed bundle quantities and costs, consumes the public `civ-engine/browser` export instead of sibling `dist` paths, keeps the local engine lock at 2.4.1, lints its executable playtest scripts, and documents the actual 5175 controls/bootstrap path. Focused persistence/browser contracts, typecheck, lint, and both dependency audits are green.
- 2026-07-10: Recursive playtest candidate `visual-loop-ended-with-guidance` closed fixed-proven. Ordinary visual loops now default to 80 decisions; recursive passes originally used 120 and now use the 140-step Harvest Hearth curriculum unless explicitly bounded. Guided paint actions reselect Plot after another tool is active. A controlled 64-step baseline reproduced the finding; the original proof run exercised Land -> Plot -> paint, agent-stopped at 108/120, resolved the candidate, and replay-checked 6 segments with zero skips or divergences. Commit `6e8c467` pins the pure decision and guided-paint contracts; the current budget contract is pinned by the 2026-07-12 recursive hardening tests.
- 2026-07-08: playtest:recursive - one-command proposal-only pass (fleet-uniform outcome vocabulary no-fix-candidate|proposal-only|run-failed): runs the visual loop, selects the top open fix-classified finding, writes latest.pass-manifest.json + passes.jsonl; the driving agent is the fix arm (fix, rerun, compare). Archive step hardened against Windows EPERM dir locks (retry + per-child move fallback). Smoke: found a real candidate (step-cap-with-guidance) and honestly reported the vacuous short-run self-check as weak.
- 2026-07-08: Recursive-loop alignment - dev-only in-page SessionRecorder + `__farmDebug.exportBundle()` (terminal-snapshot export), visual loop writes/verifies `latest.bundle.json` via replay self-check with the recorded seed, findings author unverified and flip to verified-by-metric with bundle evidence only under strong verification, append-only run history + `ledger.jsonl`, replay script repointed to the live bundle. Review fixed a dead verification path (engine skippedSegments is an array), a seed-divergent world factory, and vacuous live exports; rode on engine v1.6.1 (browser-safe session ids).
- 2026-07-04: Captured the initial product direction for a desktop-first top-down pixel idle farm with autonomous workers, deterministic simulation, `civ-engine`, Phaser, DOM UI, and full docs/workflow expectations.
- 2026-07-04: Built the first playable vertical slice with `civ-engine` simulation, Phaser rendering, DOM HUD/panels, hotkeys, autosave, selling, seed buying, undo/redo, debug APIs, and Playwright screenshot smoke evidence.
- 2026-07-04: Review pass fixed live browser tick accumulation, storage-capacity recalculation after storage removal, and added a collapsible side panel.
- 2026-07-04: Added global tool upgrades for worker movement and watering efficiency so the documented tool-upgrade mechanic is playable.
- 2026-07-04: Moved tier definitions into shared content and rendered the full tier path in the Goals panel.
- 2026-07-04: Ran a UI/pixel-art playtest loop, tightened button chrome, improved pixel tile/worker rendering, and reordered Goals content around actionable upgrades.
- 2026-07-04: Added denser object-level pixel details for farm tiles, crops, buildings, workers, cargo, locked land, and path surfaces.
- 2026-07-04: Smoothed worker rendering by easing visual positions along simulation path-progress targets instead of drawing only at tile centers.
- 2026-07-04: Re-skinned UI chrome and broad backdrops from green farm panels to compact black-and-white glass while preserving colorful farm objects.
- 2026-07-04: Removed path tiles as a farm concept, normalized legacy path saves to empty land, removed the Path tool, and deleted all browser sound effects.
- 2026-07-04: Changed tier progression from automatic rewards to player-claimed unlocks, tightened button sizing, added icon-led controls, and removed the tiny Vite config so sandboxed verification can run.
- 2026-07-04: Made Plot painting non-destructive so it only converts empty owned land and cannot replace wells, storage bins, or existing plots.
- 2026-07-04: Added an LLM playtest harness with browser screenshots, structured findings, improvement annotations, civ-engine replay bundle capture, and a replay inspector.
- 2026-07-04: Extended the LLM playtest loop to catch idle no-seed stalls, added HUD guidance, and placed direct seed-buy actions in the Goals panel when farmers are waiting.
- 2026-07-05: Added first-time next-click tutorial tips, crop/seed icons, and plot-space guidance so seed-rich idle workers tell the player to paint more plots.
- 2026-07-05: Converted UI controls to crisp pixel-art glyphs and fixed a worker-trap case where blocking buildings could strand workers on occupied tiles in existing saves.
- 2026-07-05: Moved starter storage to an outside utility tile, recovered older no-storage starter saves, and made all placement tools non-overwriting until the player bulldozes the existing cell content.
- 2026-07-05: Fixed tutorial tip dismissal so the close button hides the currently visible paired guidance tip and added browser regression coverage.
- 2026-07-05: Recolored UI pixel icons with small per-icon palettes so controls stay compact but read as colorful farm-game graphics.
- 2026-07-05: Generated a pixel-art icon concept sheet, scaled the desktop UI up modestly, and revised icon masks toward more recognizable object silhouettes.
- 2026-07-05: Added a draggable, persisted side-panel width preference so text-heavy panel content can be widened without changing the deterministic farm save.
- 2026-07-05: Capped bottom toolbar button widths so the larger readable controls remain compact on desktop.
- 2026-07-05: Fixed duplicate worker plot targeting so extra farmhands reserve separate planting, watering, and harvesting jobs when enough work is available.
- 2026-07-05: Reworked the LLM browser playtest harness so browser scenarios are controlled through visible player actions and reports include observations/actions an LLM can review.
- 2026-07-05: Added a step-by-step LLM visual playtest loop with screenshot observations, visible action extraction, player-only execution, optional external decision provider, and an HTML replay viewer.
- 2026-07-05: Extended the visual loop to exercise the first Worker Boots upgrade, disabled empty or unaffordable economy actions, and clamped tutorial tips so canvas and panel prompts remain visible in screenshots.
- 2026-07-05: Polished the UI/graphics pass with a warm-charcoal stage, HUD stat chips, surfaced panel rows, cleaner button states, richer tile contrast, icon-only side-panel tabs, compact icon-plus-key toolbar controls, and SVG-safe click handling.
- 2026-07-05: Expanded the LLM/player browser playtest so it touches every visible control surface, added drag/range actions to the visual loop, fixed seed guidance when Inventory is already open, and made reload autosave checks preserve the saved farm.
- 2026-07-05: Persisted the selected speed as a separate UI preference so reloading an idle farm keeps the player's 2x or 4x pacing without changing the deterministic farm save.
- 2026-07-05: Moved side-panel tutorial cards beside the panel so seed, sell, and tier guidance no longer covers the controls it asks players to use.
- 2026-07-05: Added readable action labels to Crop Mix sliders so the LLM/player reports identify them as crop-specific controls instead of anonymous inputs.
- 2026-07-05: Extended the scripted LLM player surface tour to exercise camera panning and wheel zoom, matching the documented player controls.
- 2026-07-05: Extended the step-by-step visual LLM loop so the action schema and local heuristic exercise camera panning and mouse-wheel zoom through player-facing controls.
- 2026-07-05: Fixed a short-lived Inventory panel stale-render case where HUD storage and tutorial guidance could update before visible crop rows did.
- 2026-07-05: Raised the step-by-step visual LLM loop budget so the default run completes the first visible tier claim instead of ending one action early.
- 2026-07-05: Reworked transient tutorial tips into stable Farm Guide cards with consistent Do/Why sections, richer context, and a readable hold when guidance changes.
- 2026-07-06: Added seed stock, planted counts, and readiness states to Crop Mix rows after the visual LLM loop exposed that percentages alone did not explain planting blockers.
- 2026-07-06: Fixed stale Crop Mix guidance so opening the panel under another active guide prevents a later redundant Tune Crop Mix card.
- 2026-07-06: Labeled locked seed purchases as Locked in Inventory after fresh-start screenshots showed disabled Wheat/Tomato seed prices lacked context.
- 2026-07-06: Added current/target harvest counts to HUD and Goals milestone text so waiting and worker progress are visible.
- 2026-07-06: Moved Farm dev and LLM playtest defaults away from the AoE localhost port and documented `FARM_PLAYTEST_URL` for 5175 visual checks.
- 2026-07-06: Prioritized visible tier-claim Farm Guide cards over held sell guidance after the Goals reward screenshot pointed players back to Inventory.
- 2026-07-06: Tightened the visual-loop seed heuristic after the step log showed a Sell Crops guide being overridden by incidental "coins buy seeds" copy.
- 2026-07-06: Corrected the Select Plot Farm Guide shortcut after the compact screenshot showed the card saying 2 while the toolbar mapped Plot to 1.
- 2026-07-06: Added direct numeric Crop Mix percentage inputs, a persistent 100% allocation summary, and LLM harness coverage for filling the new controls.
- 2026-07-07: Re-inspected the LLM/player harness action surface and enriched both reports with canvas/numeric controls, action hints, active states, shortcuts, and input values.
- 2026-07-07: Added side-panel scroll state affordances after visual-loop Goals screenshots made dense lower panel content look clipped at the toolbar edge.
- 2026-07-07: Added a dedicated scripted LLM Inspect-panel screenshot after a visible tile selection so playtest review can judge object details directly.
- 2026-07-07: Expanded the Inspect panel from raw tile kind text into labeled object/worker detail rows after the new screenshot showed well inspection was too sparse.
- 2026-07-07: Tightened the LLM/player harness so visible text is viewport/scroll clipped, side-panel wheel actions do not count as camera zoom, and default visual loops still reach Crop Mix numeric adjustment.
- 2026-07-07: Exposed Arrow/WASD camera panning and Space pause/resume as keyboard actions in LLM/player observation packets and reports.
- 2026-07-07: Fixed the visual-loop HTML replay viewer so screenshot frames stay visible while long metadata scrolls.
- 2026-07-07: Re-audited the player-control surface and added visible toolbar shortcuts to LLM/player keyboard observations, with the visual loop pressing the Plot shortcut from the guide path.
- 2026-07-07: Added canvas drag-paint coverage to the LLM/player harness so drag tools can be exercised from visible screenshot coordinates.
- 2026-07-07: Re-inspected keyboard parity in the LLM/player harness, constrained visual-loop press decisions to listed controls, and exposed focus-required resizer/range key actions.
- 2026-07-07: Extended the default visual LLM-player heuristic to exercise the focused side-panel resize keyboard path, not only list it.
- 2026-07-07: Removed contradictory Inventory-specific seed-stall HUD copy after the visual loop showed Goals guidance as the direct restock path.
- 2026-07-07: Re-audited the LLM/player harness visibility model so observations include all hit-test-reachable controls, ignore occluded text/actions, and expose number-input arrow controls.
- 2026-07-07: Prioritized active milestone crops in Goals seed-restock guidance after the visual loop spent Tier 2 coins on Carrot before Wheat.
- 2026-07-07: Extended the visual LLM-player loop so it keeps following actionable Farm Guide/HUD guidance after tier claims instead of ending while the screen still asks for a player action.
- 2026-07-07: Made storage-pressure selling guidance recur after the visual loop ended on Goals with low coins, high stored crops, and no obvious next action.
- 2026-07-07: Taught the visual LLM-player loop to use visible zero-seed Inventory buy buttons before stopping, matching what a player can infer from the panel.
- 2026-07-07: Required the visual LLM-player loop to observe a watch frame after recent player actions before stopping, preventing post-action guidance from being missed.
- 2026-07-07: Removed fixed visible-text caps from scripted and visual LLM/player observations so the harness sees the full viewport-visible text.
- 2026-07-07: Made visible Inventory restock choices prefer the active milestone crop after the visual loop showed Wheat progress but kept buying Carrot first.
- 2026-07-07: Let visible tier-ready HUD prompts reopen Goals after the visual loop otherwise waited in Inventory when Tomato Rows was ready to claim.
- 2026-07-07: Re-inspected the LLM-player harnesses end to end, confirmed live DOM control coverage, added absolute screenshot file paths to provider/review packets, and synchronized DOM observation with rendered frames.
- 2026-07-07: Added a Tomato Rows Crop Mix guide and taught the visual LLM-player loop to open Crop Mix and type into the Tomato percentage field after Tier 3 unlocks.
- 2026-07-07: Improved visual-loop seed restocking from visible Inventory rows so later zero-stock crops such as Tomato are exercised instead of repeatedly buying stocked starter seeds.
- 2026-07-07: Re-inspected the visual LLM-player loop under a 120-step progression run, made capped actionable guidance a finding, and retargeted plot clicks to open owned land bands visible in the screenshots.
- 2026-07-07: Added visible hover/focus labels for icon-only side-panel tabs and extended the LLM/player harnesses with a hover action so pointer-only affordances are playtested.
- 2026-07-07: Reused `civ-engine`'s visual playtest runner for the step-by-step LLM-player loop, with Farm-specific adapters preserving screenshot capture, player controls, local heuristics, provider prompts, and report artifacts.
- 2026-07-07: Tightened the visual LLM-player late-game heuristic so open-ended Tier 3 audits do not run to the cap by repeatedly selling tiny non-urgent harvest trickles.
- 2026-07-07: Added hover/focus labels above compact icon-only toolbar buttons so smaller desktop layouts keep speed/tool controls understandable without widening the toolbar.
- 2026-07-07: Made Inventory seed-restock Farm Guide cards name and target the active milestone crop when its seed row is buyable.
- 2026-07-07: Guarded scripted and visual LLM playtest storage setup after an intermittent restricted-document storage error closed the browser during the surface tour.
- 2026-07-07: Taught the visual LLM-player to obey goal-specific seed guide cards after a default run clicked Carrot while the visible guide asked for Wheat.
- 2026-07-07: Raised the default visual LLM-player loop budget so post-tier Crop Mix restocking fits inside ordinary runs.
- 2026-07-07: Muted disabled Inventory sell-button coin glyphs after a late-game visual screenshot made zero-count crop sale controls read too similarly to active sale buttons.
- 2026-07-07: Reworded terminal Tomato Rows HUD and Goals copy after visual replay showed the endgame objective was vague and the card still promised a next tier claim.
- 2026-07-07: Deprecated the old scripted LLM playtest harness and made `playtest:llm` delegate to the canonical visual screenshot/action loop with a deeper default audit budget.
- 2026-07-07: Expanded the canonical visual LLM-player loop to audit the retired scripted surface controls and flag clean stops when final screenshots still show actionable guidance.
- 2026-07-08: Standardized visual-loop self-improvement reports on `civ-engine` `ImprovementFinding` objects, added run manifests, visual-finding bridges, before/after rerun comparison, and dogfooded provider-failure recovery evidence.
- 2026-07-08: Made Farm Guide cards stay on the current suggested action until the player follows it or dismisses it, while raising the desktop UI text/control scale for readability.
